{
  "name": "enterprise-authentication-security",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise-level authentication & security module â€” full-stack application",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "vercel-build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "@netlify/functions": "^6.0.0",
    "serverless-http": "^4.0.0"
  }
}
